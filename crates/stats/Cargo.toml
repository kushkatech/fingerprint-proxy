[package]
name = "fingerprint-proxy-stats"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
fingerprint-proxy-core = { path = "../core" }
fingerprint-proxy-stats-api = { path = "../stats-api" }
