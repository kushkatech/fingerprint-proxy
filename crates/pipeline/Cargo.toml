[package]
name = "fingerprint-proxy-pipeline"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
fingerprint-proxy-core = { path = "../core" }

[dev-dependencies]
fingerprint-proxy-prepipeline = { path = "../prepipeline" }
